import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'GF_FRONT_APP_GIT',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
